import React from 'react'
export default function DataSettings(){
  return (
    <div className="section">
      <h2>Data settings</h2>
      <p>Import from Excel, export CSV/JSON, backups (placeholder).</p>
    </div>
  )
}
